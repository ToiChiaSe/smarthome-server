<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>SmartHome Dashboard</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <nav>
    <a href="/dashboard">Dashboard</a>
    <a href="/thresholds">Thresholds</a>
    <a href="/schedules">Scheduler</a>
    <a href="/users">Users</a>
    <form method="post" action="/api/auth/logout" style="display:inline;">
      <button class="btn-link">Logout (<%= user.username %>)</button>
    </form>
  </nav>
  <div class="container">
    <h1>SmartHome Dashboard</h1>
    <section>
      <h2>Thiết bị</h2>
      <table>
        <thead><tr><th>ID</th><th>Type</th><th>Room</th><th>State</th></tr></thead>
        <tbody>
          <% devices.forEach(d=>{ %>
            <tr>
              <td><%= d.deviceId %></td>
              <td><%= d.type %></td>
              <td><%= d.room %></td>
              <td><%= d.state.on ? "ON" : "OFF" %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </section>
  </div>
</body>
</html>

